# Third-Party Vendors

This directory is reserved for read-only vendor trees.

Planned vendors:

- `picocalc-text-starter`
- `sdltrs`

Preferred import method:

- `git submodule`

Do not place local project code in this directory.
